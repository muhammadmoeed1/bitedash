# Only needed for production builds. In dev, leave this unset — Vite proxies /api and
# /socket.io to the backend at http://localhost:6006 (see vite.config.ts).
# In production, set this to the deployed backend origin, e.g. https://api.bitedash.example.com
VITE_API_URL=
